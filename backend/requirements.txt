# Runtime dependencies — direct only.
# Transitive packages (asgiref, kombu, billiard, PyJWT, ...) are resolved by pip
# and deliberately not pinned here.

Django==5.2.16
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1

psycopg[binary]==3.3.4

celery[redis]==5.6.3
redis==6.4.0

django-environ==0.14.0
gunicorn==26.0.0
